import { ReactNode } from 'react';
import { ISectionData } from './types';

export function reArrangeSections(
  children: ReactNode,
  oldSectionsData: Record<string, ISectionData>
): Record<string, ISectionData> {
  return {};
}
