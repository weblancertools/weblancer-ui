import { FunctionComponent } from 'react';
import layoutStyle from '../../../styles/editorLayout.module.scss';

export const Header: FunctionComponent = () => {
  return <div className={layoutStyle.header}>Header</div>;
};
